server:
  port: 8083

spring:
  application:
    name: hotels-service
  jpa:
    hibernate:
      ddl-auto: update
    database-plataform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/hotels_service?useSSL=false&serverTimezone=UTC
    username: usercity
    password: userCity1978

eureka:
  client:
    service-url-defaultZone: eureka-server:8761/eureka

